@{
  Layout = "_Layout";
}

@using AnimalShelter.Models;

<h1>Animals</h1>

@if (@Model.Count == 0)
{
  <h3>No sacrifices have been offered to the gods, you peasants.</h3>
}

@* <div>
  @foreach(Animal animal in Model)
  {
    <p>@Html.ActionLink($"{animal.Name}", "Details", new { id = animal.AnimalId })</p>
  }
</div> *@

<p>@Html.ActionLink("Add new item", "Create")</p>